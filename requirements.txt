# Pinned to exact stable versions (rule 3.7)
aiogram==3.15.0
aiosqlite==0.20.0
python-dotenv==1.0.1

# Optional: persistent FSM storage (enable by setting REDIS_URL)
redis==5.2.1

# Dev / test
pytest==8.3.4
pytest-asyncio==0.24.0
